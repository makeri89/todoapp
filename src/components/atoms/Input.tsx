import { styled } from '@lib/stitches.config'

export const Input = styled('input', {
  all: 'unset',
  backgroundColor: '$plum12',
  borderRadius: 5,
  padding: '0.5rem',
  color: '$plum7',
  display: 'inline-flex',
  minWidth: 120,
  height: 20,
})
